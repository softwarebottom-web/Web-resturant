name: Unzip app.zip
on: push
jobs:
  unzip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Extract app.zip
        run: |
          unzip -o app.zip
          rm app.zip
      - name: Push hasil extract
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "Auto extracted app.zip 🗿" || exit 0
          git push
